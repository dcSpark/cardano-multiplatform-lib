# Chain

This is the core cardano-multiplatform-lib crate for all on-chain data types.
This was generated from the `specs/conway/` CDDL specs using cddl-codegen.
Regenerate via `./codegen.sh chain` from the repo root (see `specs/README.md`).
